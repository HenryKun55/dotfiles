# ── Core tools ──────────────────────────────────────────────────────
brew "neovim"
brew "tmux"
brew "ripgrep"
brew "lazygit"
brew "git"
brew "gh"
brew "jq"
brew "fzf"

# ── Shell ───────────────────────────────────────────────────────────
brew "powerlevel10k"
brew "zsh-syntax-highlighting"
brew "zsh-autosuggestions"
brew "zoxide"
brew "antigen"

# ── Runtimes ────────────────────────────────────────────────────────
brew "fnm"
brew "pyenv"
brew "rbenv"
# openjdk@17: opt-in via install.sh (perguntado, ~500MB, só pra Android/JVM dev)
# bun: instalado via install.sh (curl) — não tem fórmula oficial

# ── Dev tools ───────────────────────────────────────────────────────
brew "prettierd"
brew "lua-language-server"
brew "tree-sitter-cli"

# ── C/C++ ───────────────────────────────────────────────────────────
# clang/clang++/clangd/make/lldb vêm do Xcode Command Line Tools (xcode-select
# --install), não do brew. Aqui só o que é brew-only:
brew "cmake"
brew "ninja"

# ── Casks ───────────────────────────────────────────────────────────
cask "alacritty"
cask "font-meslo-lg-nerd-font"
cask "iterm2"

# ── Optional: Flutter/Mobile (uncomment if needed) ──────────────────
# brew "cocoapods"
# cask "android-studio"
# cask "flutter"
